skill_name: snowolf-ssh-images-upload
description: 根据架构自动ssh上传images镜像包
entry: script/main.py
python_env: default
inputs:
  - name: env_name
    type: string
  - name: version
    type: string
outputs:
  - name: arch
  - name: package_local_path
context_transfer: true
lifecycle:
  timeout: 600
retry: 0
