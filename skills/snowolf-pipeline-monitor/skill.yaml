skill_name: snowolf-pipeline-monitor
description: 流水线状态监控
entry: script/main.py
python_env: default
inputs:
  - name: env_name
    type: string
  - name: version
    type: string
outputs:
  - name: arch
  - name: package_local_path
context_transfer: true
lifecycle:
  timeout: 600
retry: 0
